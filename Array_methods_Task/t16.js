// Count negative numbers in a numeric array.
const arr = [-5, 10, -2, 8, 0];


result = arr.filter(num=> num<0)
console.log(result.length);


// Remove Negative Numbers
const ages = [-5, 10, -2, 8, 0];


result = ages.filter(num=> num>0)
console.log(result);
